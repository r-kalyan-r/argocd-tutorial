apiVersion: batch/v1
kind: Job
metadata:
  name: failed-job
spec:
  backoffLimit: 0  # Prevent Kubernetes from retrying on its own
  template:
    spec:
      containers:
      - name: check
        image: nginx:latest
        command: ["some-nonexistent-command"]  # ❌ Fails instantly
      restartPolicy: Never
