apiVersion: v1
kind: Service
metadata:
  name: {{ include "image-updator.fullname" . }}
  labels:
    {{- include "image-updator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "image-updator.selectorLabels" . | nindent 4 }}
