apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-updator.fullname" . }}-test-connection"
  labels:
    {{- include "image-updator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-updator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
