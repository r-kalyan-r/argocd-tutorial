apiVersion: apps/v1
kind: Deployment
metadata:
  name: prune-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prune-app
  template:
    metadata:
      labels:
        app: prune-app
    spec:
      containers:
      - name: app
        image: nginx:latest