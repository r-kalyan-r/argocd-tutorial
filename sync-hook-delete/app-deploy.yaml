apiVersion: apps/v1
kind: Deployment
metadata:
  name: delete-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: delete-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: app
        image: nginx:latest