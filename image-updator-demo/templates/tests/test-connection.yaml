apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "image-updator-demo.fullname" . }}-test-connection"
  labels:
    {{- include "image-updator-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "image-updator-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
